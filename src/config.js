const config = {
  api: {
    baseUrl: 'https://github-scanner-36faf018c358.herokuapp.com/repos',
    endpoints: {
      repos: '/',
      reposBatch: '/batch'
    },
  },
};

export default config; 
